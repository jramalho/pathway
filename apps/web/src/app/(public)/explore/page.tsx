import { PublicPageContainer } from '@/components/public/public-page-container';
import { PlaceholderSection } from '@/components/public/placeholder-section';

export default function ExplorePage() {
  return (
    <PublicPageContainer>
      <PlaceholderSection
        heading="Explore"
        description="Browse all learning paths and lessons. Search and topic filters arrive soon."
      />
    </PublicPageContainer>
  );
}
